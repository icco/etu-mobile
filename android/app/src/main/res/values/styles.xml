<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowBackground">@color/etu_background</item>
        <item name="android:statusBarColor">@color/etu_background</item>
        <item name="android:navigationBarColor">@color/etu_background</item>
        <item name="android:windowLightStatusBar">@bool/etu_light_bars</item>
        <item name="android:windowLightNavigationBar">@bool/etu_light_bars</item>
        <item name="android:colorAccent">@color/etu_accent</item>
    </style>

</resources>
